# Node
node_modules/
dist/
dist-electron*/

# Old build output variants (keep only dist-electron-v4)
client/dist-electron/
client/dist-electron-fresh/
client/dist-electron-v2/
client/dist-electron-v3/

# Environment / secrets
.env
.env.*
.env.local
.env.production
.env*.local

# Env files for firebase functions
firebase/functions/.env.local
firebase/functions/.env.*.local

# Service Account Keys
*-adminsdk*.json
gen-lang-client*.json
serviceAccountKey.json

# User/credential dumps (contain password hashes + PII — never commit)
# NOTE: these are still tracked in history; run `git rm --cached` + history rewrite to fully purge.
firebase/auth_users.json
firebase/creator_import.json
_kaynak_kod_yedek/firebase/auth_users.json
_kaynak_kod_yedek/firebase/creator_import.json

# Firebase emulator data & cache
firebase/emulator-data/
.firebase/

# Build artifacts
builds/
*.exe

# Large error/debug artifacts
deploy_error.txt
firestore-debug.log
build-error.log
build_error.txt
build_error2.txt
build.log
error.log

# Legacy data (contains PII)
Eski bir db/

# OS / IDE
*.log
nul
.venv/
.DS_Store
Thumbs.db
.idea/
.vscode/

# Python
__pycache__/
*.pyc

gh-releases.js
